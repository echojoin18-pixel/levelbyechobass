# Level Egypt — Static Clone

A static mirror of the deployed Level Egypt SPA (`level-egypt-website-85vlf98y.devinapps.com`).

These are the prebuilt assets (HTML, JS, CSS, images) served as-is.
To preview locally:

```bash
cd static-clone
python3 -m http.server 8080
# then open http://localhost:8080
```
